{
  "name": "canvas-fill",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "nodemon --watch 'src/**/*.ts' --ignore 'src/**/*.spec.ts' --exec 'ts-node' src/index.ts",
    "dev": "nodemon --watch 'public/src/*.ts' --exec 'npm run test'",
    "test": "webpack --watch",
    "build": "tsc",
    "heroku-prebuild": "npm run heroku:build",
    "heroku:build": "npm install --only=dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "nodemon": "^2.0.4",
    "ts-node": "^9.0.0",
    "webpack": "^4.44.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.8",
    "@types/node": "^14.11.1",
    "ts-loader": "^8.0.4",
    "typescript": "^4.0.3",
    "webpack-cli": "^3.3.12"
  }
}
